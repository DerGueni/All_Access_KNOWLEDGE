<Page
    x:Class="ConsysWinUI.Views.DienstplanObjektView"
    x:Name="Page"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:ConsysWinUI.ViewModels"
    xmlns:controls="using:ConsysWinUI.Controls"
    mc:Ignorable="d"
    Background="#F0F0F0">

    <!-- 1:1 Nachbildung von Access-Formular: frm_DP_Dienstplan_Objekt -->

    <Page.Resources>
        <!-- Sidebar Button Style - Access-Look -->
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#A05050"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="CornerRadius" Value="0"/>
        </Style>

        <!-- Export Button Style - Access Rosa/Beige (#D5A5D7) -->
        <!-- Access Original: ca. 126x22px (1890x330 Twips) -->
        <Style x:Key="ExportButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#D5A5D7"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D5A5D7"/>
            <Setter Property="CornerRadius" Value="0"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Width" Value="126"/>
            <Setter Property="Height" Value="22"/>
        </Style>

        <!-- Navigation Button Style (Vor/Zurueck) -->
        <!-- Access Original: 38x15px (567x224 Twips) -->
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="CornerRadius" Value="0"/>
            <Setter Property="Width" Value="38"/>
            <Setter Property="Height" Value="15"/>
            <Setter Property="Padding" Value="0"/>
        </Style>

        <!-- Header Label Style - Access Hellbraun (#C5A565) -->
        <!-- Hinweis: TextBlock hat keine Background-Property, verwende Border für Hintergrund -->
        <Style x:Key="HeaderLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <!-- Day Header Style - Access Hellrosa/Beige (#F6D0F2) -->
        <Style x:Key="DayHeaderStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="185"/>  <!-- Sidebar - Access Original: 185px -->
            <ColumnDefinition Width="*"/>    <!-- Hauptbereich -->
        </Grid.ColumnDefinitions>

        <!-- ============================================ -->
        <!-- SIDEBAR - Dunkelrot (#8B0000) wie Access -->
        <!-- ============================================ -->
        <Border Grid.Column="0" Background="#8B0000">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- Logo/Titel - Weiße Box mit schwarzem Rahmen wie Access Original -->
                    <Border Background="White" BorderBrush="Black" BorderThickness="1"
                            Margin="8,10" Padding="8,3">
                        <TextBlock Text="HAUPTMENÜ"
                                   Foreground="Black"
                                   FontWeight="Bold"
                                   FontSize="11"
                                   HorizontalAlignment="Center"/>
                    </Border>

                    <!-- Menü-Buttons -->
                    <Button Content="Dienstplanübersicht" Style="{StaticResource SidebarButtonStyle}" Margin="5,2"
                            Command="{x:Bind ViewModel.NavigateDienstplanCommand, Mode=OneWay}"/>
                    <Button Content="Planungsübersicht" Style="{StaticResource SidebarButtonStyle}" Margin="5,2"
                            Background="#D4A574" Foreground="Black"
                            Command="{x:Bind ViewModel.NavigatePlanungCommand, Mode=OneWay}"/>
                    <Button Content="Auftragsverwaltung" Style="{StaticResource SidebarButtonStyle}" Margin="5,2"
                            Command="{x:Bind ViewModel.NavigateAuftragCommand, Mode=OneWay}"/>
                    <Button Content="Mitarbeiterverwaltung" Style="{StaticResource SidebarButtonStyle}" Margin="5,2"
                            Command="{x:Bind ViewModel.NavigateMitarbeiterCommand, Mode=OneWay}"/>
                    <Button Content="Offene Mail Anfragen" Style="{StaticResource SidebarButtonStyle}" Margin="5,2"
                            Command="{x:Bind ViewModel.NavigateAnfragenCommand, Mode=OneWay}"/>
                    <Button Content="Excel Zeitkonten" Style="{StaticResource SidebarButtonStyle}" Margin="5,2"
                            Command="{x:Bind ViewModel.NavigateExcelZeitkontenCommand, Mode=OneWay}"/>
                    <Button Content="Zeitkonten" Style="{StaticResource SidebarButtonStyle}" Margin="5,2"
                            Command="{x:Bind ViewModel.NavigateZeitkontenCommand, Mode=OneWay}"/>
                    <Button Content="Abwesenheitsplanung" Style="{StaticResource SidebarButtonStyle}" Margin="5,2"
                            Command="{x:Bind ViewModel.NavigateAbwesenheitCommand, Mode=OneWay}"/>
                    <Button Content="Dienstausweis erstellen" Style="{StaticResource SidebarButtonStyle}" Margin="5,2"
                            Command="{x:Bind ViewModel.NavigateDienstausweisCommand, Mode=OneWay}"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- ============================================ -->
        <!-- HAUPTBEREICH -->
        <!-- ============================================ -->
        <Grid Grid.Column="1" Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header mit Titel und Auftrag-Info -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Vertical" Spacing="4">
                <TextBlock Text="Dienstplan Objekt" Style="{StaticResource TitleTextBlockStyle}"/>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock Text="{x:Bind ViewModel.SelectedAuftragName, Mode=OneWay, FallbackValue='Kein Auftrag ausgewählt'}"
                               Style="{StaticResource BodyTextBlockStyle}"
                               FontWeight="SemiBold"/>
                    <TextBlock Text="•" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Text="{x:Bind ViewModel.ObjektName, Mode=OneWay}"
                               Style="{StaticResource BodyTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Text="•" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Text="{x:Bind ViewModel.VeranstalterName, Mode=OneWay}"
                               Style="{StaticResource BodyTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
            </StackPanel>

            <!-- Action Buttons - Access Rosa/Beige Style -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <Button Command="{x:Bind ViewModel.OpenAuftragCommand, Mode=OneWay}"
                        Style="{StaticResource ExportButtonStyle}"
                        ToolTipService.ToolTip="Auftragstamm öffnen">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <FontIcon Glyph="&#xE8A5;" FontSize="14"/>
                        <TextBlock Text="Auftrag"/>
                    </StackPanel>
                </Button>
                <Button Command="{x:Bind ViewModel.ExportCommand, Mode=OneWay}"
                        Style="{StaticResource ExportButtonStyle}"
                        ToolTipService.ToolTip="Exportieren">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <FontIcon Glyph="&#xEDE1;" FontSize="14"/>
                        <TextBlock Text="Export"/>
                    </StackPanel>
                </Button>
                <Button Command="{x:Bind ViewModel.PrintCommand, Mode=OneWay}"
                        Style="{StaticResource ExportButtonStyle}"
                        ToolTipService.ToolTip="Drucken">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <FontIcon Glyph="&#xE749;" FontSize="14"/>
                        <TextBlock Text="Drucken"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Filter-Bereich -->
        <Border Grid.Row="1"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="16"
                Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Auftrag Auswahl -->
                <StackPanel Grid.Column="0" Spacing="4" Margin="0,0,16,0">
                    <TextBlock Text="Auftrag / Objekt" FontWeight="SemiBold" FontSize="12"/>
                    <ComboBox ItemsSource="{x:Bind ViewModel.AuftragListe, Mode=OneWay}"
                              SelectedValue="{x:Bind ViewModel.SelectedVaId, Mode=TwoWay}"
                              SelectedValuePath="VaId"
                              DisplayMemberPath="DisplayName"
                              PlaceholderText="Auftrag wählen..."
                              Width="350"/>
                </StackPanel>

                <!-- Filter: Nur unbesetzte -->
                <CheckBox Grid.Column="1"
                          Content="Nur unbesetzte Schichten"
                          IsChecked="{x:Bind ViewModel.NurUnbesetzte, Mode=TwoWay}"
                          Command="{x:Bind ViewModel.FilterChangedCommand, Mode=OneWay}"
                          VerticalAlignment="Bottom"
                          Margin="0,0,16,8"/>

                <!-- Spacer -->
                <StackPanel Grid.Column="2"/>

                <!-- Load Button -->
                <StackPanel Grid.Column="3" Spacing="4">
                    <TextBlock Text=" " FontSize="12"/>
                    <Button Command="{x:Bind ViewModel.LoadDienstplanCommand, Mode=OneWay}"
                            Style="{StaticResource AccentButtonStyle}"
                            ToolTipService.ToolTip="Dienstplan laden">
                        <StackPanel Orientation="Horizontal" Spacing="4">
                            <FontIcon Glyph="&#xE72C;" FontSize="14"/>
                            <TextBlock Text="Laden"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Status + Statistik Bar -->
        <Border Grid.Row="2"
                Background="{ThemeResource SystemFillColorAttentionBackgroundBrush}"
                BorderBrush="{ThemeResource SystemFillColorAttentionBrush}"
                BorderThickness="1"
                Padding="12,8"
                CornerRadius="4"
                Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Loading Status -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8">
                    <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" Width="16" Height="16"/>
                    <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay, FallbackValue='Bereit'}"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Statistik -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="16">
                    <!-- Schichten -->
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <TextBlock Text="Schichten:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        <TextBlock Text="{x:Bind ViewModel.AnzahlSchichten, Mode=OneWay, FallbackValue='0'}"
                                   FontWeight="Bold"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- MA Soll -->
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <TextBlock Text="MA Soll:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        <TextBlock Text="{x:Bind ViewModel.MaGesamt, Mode=OneWay, FallbackValue='0'}"
                                   FontWeight="Bold"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- MA Ist -->
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <TextBlock Text="MA Ist:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        <TextBlock Text="{x:Bind ViewModel.MaZugeordnet, Mode=OneWay, FallbackValue='0'}"
                                   FontWeight="Bold"
                                   Foreground="{ThemeResource SystemFillColorSuccessBrush}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- MA Fehlt -->
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <TextBlock Text="Fehlen:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        <TextBlock Text="{x:Bind ViewModel.MaFehlt, Mode=OneWay, FallbackValue='0'}"
                                   FontWeight="Bold"
                                   Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Besetzungsgrad -->
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <TextBlock Text="Besetzung:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        <TextBlock Text="{x:Bind ViewModel.BesetzungsgradText, Mode=OneWay, FallbackValue='0.0'}"
                                   FontWeight="Bold"
                                   VerticalAlignment="Center"/>
                        <TextBlock Text="%" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- View-Toggle -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8" Margin="0,0,0,12">
                <RadioButton Content="Kalender-Ansicht"
                             IsChecked="True"
                             GroupName="ViewMode"
                             Tag="Calendar"
                             Click="ViewMode_Changed"/>
                <RadioButton Content="Listen-Ansicht"
                             GroupName="ViewMode"
                             Tag="List"
                             Click="ViewMode_Changed"/>
            </StackPanel>

            <!-- Kalender Grid -->
            <controls:CalendarGrid x:Name="CalendarGrid"
                                   Grid.Row="1"
                                   Visibility="Visible"
                                   EntryClicked="CalendarGrid_EntryClicked"
                                   DateRangeChanged="CalendarGrid_DateRangeChanged"/>

            <!-- Listen-Ansicht (Original) -->
            <Grid x:Name="ListViewGrid" Grid.Row="1" Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Schichten Liste -->
            <Border Grid.Column="0"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="Schichten"
                               Style="{StaticResource SubtitleTextBlockStyle}"
                               Margin="0,0,0,12"/>

                    <ListView Grid.Row="1"
                              ItemsSource="{x:Bind ViewModel.Schichten, Mode=OneWay}"
                              SelectedItem="{x:Bind ViewModel.SelectedSchicht, Mode=TwoWay}"
                              SelectionMode="Single">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="viewmodels:SchichtDetailItem">
                                <Grid Padding="12,8" ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="80"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Datum -->
                                    <TextBlock Grid.Column="0"
                                               Text="{x:Bind DatumText}"
                                               FontWeight="SemiBold"
                                               VerticalAlignment="Center"/>

                                    <!-- Zeit -->
                                    <TextBlock Grid.Column="1"
                                               VerticalAlignment="Center"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                                        <Run Text="{x:Bind VaStart}"/>
                                        <Run Text=" - "/>
                                        <Run Text="{x:Bind VaEnde}"/>
                                    </TextBlock>

                                    <!-- Bemerkung -->
                                    <TextBlock Grid.Column="2"
                                               Text="{x:Bind Bemerkung}"
                                               VerticalAlignment="Center"
                                               TextTrimming="CharacterEllipsis"/>

                                    <!-- Status -->
                                    <Border Grid.Column="3"
                                            Background="{ThemeResource SystemFillColorCautionBackgroundBrush}"
                                            CornerRadius="4"
                                            Padding="8,4"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center">
                                        <TextBlock Text="{x:Bind StatusText}"
                                                   FontSize="11"
                                                   FontWeight="SemiBold"/>
                                    </Border>

                                    <!-- MA Anzahl -->
                                    <StackPanel Grid.Column="4"
                                                Orientation="Horizontal"
                                                HorizontalAlignment="Right"
                                                VerticalAlignment="Center">
                                        <TextBlock Text="{x:Bind MaAnzahlIst}"
                                                   FontWeight="Bold"
                                                   FontSize="18"/>
                                        <TextBlock Text=" / "
                                                   FontSize="18"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                        <TextBlock Text="{x:Bind MaAnzahl}"
                                                   FontSize="18"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Margin" Value="0,2"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                    </ListView>
                </Grid>
            </Border>

            <!-- Rechte Spalte: Zugeordnete MA + Actions -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Zugeordnete Mitarbeiter -->
                <Border Grid.Row="0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16"
                        Margin="0,0,0,16">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                   Text="Zugeordnete Mitarbeiter"
                                   Style="{StaticResource SubtitleTextBlockStyle}"
                                   Margin="0,0,0,12"/>

                        <ListView Grid.Row="1"
                                  ItemsSource="{x:Bind ViewModel.ZugeordneteMitarbeiter, Mode=OneWay}"
                                  SelectionMode="Single">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="viewmodels:MaZuordnungDetailItem">
                                    <Grid Padding="8">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Name -->
                                        <TextBlock Grid.Row="0" Grid.Column="0"
                                                   Text="{x:Bind DisplayName}"
                                                   FontWeight="SemiBold"/>

                                        <!-- Remove Button -->
                                        <Button Grid.Row="0" Grid.Column="1"
                                                Command="{Binding DataContext.RemoveMitarbeiterCommand, ElementName=Page}"
                                                CommandParameter="{x:Bind}"
                                                Width="32" Height="32"
                                                Padding="0"
                                                Background="Transparent"
                                                ToolTipService.ToolTip="Mitarbeiter entfernen">
                                            <FontIcon Glyph="&#xE711;" FontSize="12" Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                                        </Button>

                                        <!-- Telefon -->
                                        <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                                   Text="{x:Bind TelMobil}"
                                                   FontSize="11"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                                        <!-- Zeiten -->
                                        <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                                                   FontSize="11"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                                            <Run Text="{x:Bind VaStart}"/>
                                            <Run Text=" - "/>
                                            <Run Text="{x:Bind VaEnde}"/>
                                        </TextBlock>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="Margin" Value="0,4"/>
                                </Style>
                            </ListView.ItemContainerStyle>
                        </ListView>

                        <!-- Leeres State -->
                        <StackPanel Grid.Row="1"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Spacing="8"
                                    Visibility="{x:Bind ViewModel.ZugeordneteMitarbeiter.Count, Mode=OneWay, Converter={StaticResource CountToEmptyVisibilityConverter}}">
                            <FontIcon Glyph="&#xE716;" FontSize="48" Foreground="{ThemeResource TextFillColorDisabledBrush}"/>
                            <TextBlock Text="Keine Mitarbeiter zugeordnet"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Action Buttons -->
                <StackPanel Grid.Row="1" Spacing="8">
                    <Button Command="{x:Bind ViewModel.OpenSchnellauswahlCommand, Mode=OneWay}"
                            Style="{StaticResource AccentButtonStyle}"
                            HorizontalAlignment="Stretch"
                            ToolTipService.ToolTip="Schnellauswahl für ausgewählte Schicht öffnen">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE8B5;" FontSize="14"/>
                            <TextBlock Text="Schnellauswahl öffnen"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
            </Grid>
        </Grid>
        </Grid>
    </Grid>
</Page>
