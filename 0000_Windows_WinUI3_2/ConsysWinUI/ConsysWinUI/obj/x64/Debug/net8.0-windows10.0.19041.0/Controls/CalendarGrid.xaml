<UserControl
    x:Class="ConsysWinUI.Controls.CalendarGrid"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Wochennavigation -->
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:ConnectionId='4' Grid.Column="0"
                                              
                    ToolTipService.ToolTip="Vorherige Woche">
                <FontIcon Glyph="&#xE76B;" FontSize="16"/>
            </Button>

            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Spacing="8">
                <TextBlock x:ConnectionId='6' x:Name="WeekRangeText"
                           Text="KW 00: 00.00.0000 - 00.00.0000"
                           Style="{StaticResource SubtitleTextBlockStyle}"
                           VerticalAlignment="Center"/>
                <Button x:ConnectionId='7' Content="Heute"
                                                 
                        ToolTipService.ToolTip="Aktuelle Woche anzeigen"/>
            </StackPanel>

            <Button x:ConnectionId='5' Grid.Column="2"
                                          
                    ToolTipService.ToolTip="Nächste Woche">
                <FontIcon Glyph="&#xE76C;" FontSize="16"/>
            </Button>
        </Grid>

        <!-- Legende -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="16" Margin="0,0,0,12" HorizontalAlignment="Center">
            <!-- Einsatz -->
            <StackPanel Orientation="Horizontal" Spacing="4">
                <Border Width="16" Height="16" Background="#90EE90" BorderBrush="Gray" BorderThickness="1" CornerRadius="2"/>
                <TextBlock Text="Einsatz" FontSize="12" VerticalAlignment="Center"/>
            </StackPanel>
            <!-- Urlaub -->
            <StackPanel Orientation="Horizontal" Spacing="4">
                <Border Width="16" Height="16" Background="#FFDAB9" BorderBrush="Gray" BorderThickness="1" CornerRadius="2"/>
                <TextBlock Text="Urlaub" FontSize="12" VerticalAlignment="Center"/>
            </StackPanel>
            <!-- Krank -->
            <StackPanel Orientation="Horizontal" Spacing="4">
                <Border Width="16" Height="16" Background="#FFA07A" BorderBrush="Gray" BorderThickness="1" CornerRadius="2"/>
                <TextBlock Text="Krank" FontSize="12" VerticalAlignment="Center"/>
            </StackPanel>
            <!-- Abwesenheit -->
            <StackPanel Orientation="Horizontal" Spacing="4">
                <Border Width="16" Height="16" Background="#FFB6C1" BorderBrush="Gray" BorderThickness="1" CornerRadius="2"/>
                <TextBlock Text="Abwesenheit" FontSize="12" VerticalAlignment="Center"/>
            </StackPanel>
            <!-- Schicht -->
            <StackPanel Orientation="Horizontal" Spacing="4">
                <Border Width="16" Height="16" Background="#ADD8E6" BorderBrush="Gray" BorderThickness="1" CornerRadius="2"/>
                <TextBlock Text="Schicht" FontSize="12" VerticalAlignment="Center"/>
            </StackPanel>
        </StackPanel>

        <!-- Wochentag-Header -->
        <Grid x:ConnectionId='2' Grid.Row="2" x:Name="HeaderGrid" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Dynamisch befüllt: Montag bis Sonntag -->
        </Grid>

        <!-- Kalender-Grid (7 Spalten für Tage) -->
        <ScrollViewer Grid.Row="3"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <Grid x:ConnectionId='3' x:Name="CalendarGridContent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Dynamisch befüllt mit Tages-Spalten -->
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>

